<?php


namespace App\Servers\NotificationRole;

use App\Models\NotificationSetting;
use Illuminate\Support\Facades\Log;

class HostStatusNotificationRole
{
    public static function check($serverInfo, $notificationInfo)
    {
        $NotificationSettingObj = NotificationSetting::query()->where();
        Log::debug("检查主机告警",["hostId"=>$serverInfo["hostId"],"result"=>""]);
    }
}